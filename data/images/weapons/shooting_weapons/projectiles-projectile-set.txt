-- This looks like callback hell (js ref?) but it isn't :D
-- also I didn't implement comment in CrappyDataReader (the -- is just for fun)
-- It just skip everything until hit desired-start

projectile-set-start
	projectile-start
		id 1
		sprite-filename piston-bullet.txt
	projectile-end
projectile-set-end